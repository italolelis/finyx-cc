---
phase: 18-router-sub-skill-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/insurance/SKILL.md
  - skills/insurance/sub-skills/health.md
autonomous: false
requirements:
  - ARCH-01
must_haves:
  truths:
    - "/finyx:insurance routes to health sub-skill when user asks about health insurance"
    - "Router SKILL.md dispatches to sub-skill prompts based on insurance type keyword"
    - "Existing health insurance flow works identically after migration to sub-skill"
  artifacts:
    - path: "skills/insurance/SKILL.md"
      provides: "Router dispatcher (~60 lines, no health logic)"
      contains: "sub-skills/"
    - path: "skills/insurance/sub-skills/health.md"
      provides: "Full health insurance flow (verbatim from old SKILL.md)"
      contains: "Phase 0: Preferences"
  key_links:
    - from: "skills/insurance/SKILL.md"
      to: "skills/insurance/sub-skills/health.md"
      via: "Read tool dispatch in Phase 1"
      pattern: "sub-skills.*health\\.md"
    - from: "skills/insurance/sub-skills/health.md"
      to: "skills/insurance/agents/"
      via: "Task tool spawning calc and research agents"
      pattern: "finyx-insurance-calc-agent"
    - from: "skills/insurance/sub-skills/health.md"
      to: "skills/insurance/references/"
      via: "CLAUDE_SKILL_DIR reference paths"
      pattern: "CLAUDE_SKILL_DIR.*references"
---

<objective>
Convert the monolithic insurance SKILL.md (~604 lines of health insurance logic) into a thin keyword-detection router (~60 lines) that dispatches to per-type sub-skill prompts. Migrate the health insurance flow verbatim to sub-skills/health.md.

Purpose: ARCH-01 -- enable per-type insurance sub-skills in later phases (19-22) without modifying the router.
Output: Router SKILL.md + health sub-skill file. /finyx:insurance continues to work identically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-router-sub-skill-migration/18-RESEARCH.md
@skills/insurance/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract health flow to sub-skill and create router SKILL.md</name>
  <files>skills/insurance/SKILL.md, skills/insurance/sub-skills/health.md</files>
  <read_first>
    - skills/insurance/SKILL.md (current 604-line monolith -- the source of truth)
    - skills/insurance/agents/finyx-insurance-calc-agent.md (verify agent name and path references)
    - skills/insurance/agents/finyx-insurance-research-agent.md (verify agent name and path references)
    - skills/insurance/references/disclaimer.md (verify path used in execution_context)
    - .planning/phases/18-router-sub-skill-migration/18-RESEARCH.md (router pattern and pitfalls)
  </read_first>
  <action>
Two file operations. Read the full current SKILL.md first as the source of truth.

A) Create skills/insurance/sub-skills/health.md:

1. Create directory: mkdir -p skills/insurance/sub-skills/
2. Write sub-skills/health.md with ALL content from the current SKILL.md, applying these 3 modifications:
   - REMOVE the YAML frontmatter block (the top section between triple-dash delimiters containing name, description, allowed-tools, disable-model-invocation). Sub-skills are plain Markdown, not registered commands.
   - REMOVE the execution_context block entirely. The router's execution_context already loads disclaimer.md and profile.json.
   - Add at the very top: "# Health Insurance Sub-skill" followed by an HTML comment: "Sub-skill loaded by router SKILL.md. All CLAUDE_SKILL_DIR paths resolve to skills/insurance/"
3. Keep ALL remaining content VERBATIM: objective, process (Phase 0 through Phase 7), error_handling, notes blocks. Do NOT modify any CLAUDE_SKILL_DIR references -- they resolve correctly from the parent skill directory.

B) Replace skills/insurance/SKILL.md with a new router file:

The router SKILL.md must have YAML frontmatter with these exact fields:
- name: finyx-insurance
- description: Insurance advisor for Germany -- routes to per-type sub-skills based on insurance type
- allowed-tools: Read, Bash, Write, Task, AskUserQuestion, WebSearch, WebFetch
- disable-model-invocation: true

The router body must contain these sections (see 18-RESEARCH.md "Router SKILL.md Skeleton" for the exact template):

1. objective block: "Route the user's insurance question to the appropriate sub-skill advisor." List 3 responsibilities: detect type, load sub-skill, preserve entry point.

2. execution_context block: Load only CLAUDE_SKILL_DIR/references/disclaimer.md and @.finyx/profile.json

3. process block with 2 phases:
   - Phase 0 Type Detection: Inspect ARGUMENTS, keyword map (case-insensitive): "health"/"kranken"/"pkv"/"gkv"/"krankenversicherung" maps to health. If not matched, use AskUserQuestion with singleSelect offering "Health insurance (Krankenversicherung -- GKV vs PKV)".
   - Phase 1 Sub-skill Dispatch: Read CLAUDE_SKILL_DIR/sub-skills/{sub_skill_type}.md, then follow all instructions from Phase 0 onward.

4. error_handling block: Handle unknown type (list currently available types) and no profile found.

5. notes block: Document router scope (pure dispatcher), tool permissions (union of sub-skill tools), and sub-skill file contract (plain Markdown, no YAML frontmatter, contains objective/process/error_handling/notes).

The router SKILL.md should be approximately 60-80 lines total. Copy the exact template from the "Router SKILL.md Skeleton" section of 18-RESEARCH.md.
  </action>
  <verify>
    <automated>
wc -l skills/insurance/SKILL.md | awk '{if ($1 &lt; 100) print "PASS: Router is " $1 " lines"; else print "FAIL: Router too large at " $1 " lines"}'
grep -c "Phase 0: Preferences" skills/insurance/sub-skills/health.md | awk '{if ($1 > 0) print "PASS: Health flow present"; else print "FAIL: Health flow missing"}'
head -1 skills/insurance/sub-skills/health.md | grep -c "^#" | awk '{if ($1 > 0) print "PASS: No YAML frontmatter"; else print "FAIL: May have YAML frontmatter"}'
grep -c "execution_context" skills/insurance/sub-skills/health.md | awk '{if ($1 == 0) print "PASS: No execution_context"; else print "FAIL: execution_context found"}'
grep -c "sub-skills/" skills/insurance/SKILL.md | awk '{if ($1 > 0) print "PASS: Router references sub-skills"; else print "FAIL: No sub-skills reference"}'
for tool in Read Bash Write Task AskUserQuestion WebSearch WebFetch; do grep -q "$tool" skills/insurance/SKILL.md; done; echo "Tool check complete"
grep -c "finyx-insurance-calc-agent" skills/insurance/sub-skills/health.md | awk '{if ($1 > 0) print "PASS: Calc agent referenced"; else print "FAIL: Calc agent missing"}'
grep -c "finyx-insurance-research-agent" skills/insurance/sub-skills/health.md | awk '{if ($1 > 0) print "PASS: Research agent referenced"; else print "FAIL: Research agent missing"}'
    </automated>
  </verify>
  <acceptance_criteria>
    - skills/insurance/SKILL.md is under 100 lines
    - skills/insurance/SKILL.md contains "name: finyx-insurance" in frontmatter
    - skills/insurance/SKILL.md contains "sub-skills/" in process section
    - skills/insurance/SKILL.md allowed-tools includes: Read, Bash, Write, Task, AskUserQuestion, WebSearch, WebFetch
    - skills/insurance/SKILL.md does NOT contain "JAEG" or "Phase 3: Health Questionnaire"
    - skills/insurance/sub-skills/health.md exists
    - skills/insurance/sub-skills/health.md does NOT start with triple-dash (no YAML frontmatter)
    - skills/insurance/sub-skills/health.md does NOT contain execution_context block
    - skills/insurance/sub-skills/health.md contains "Phase 0: Preferences" through "Phase 7: Recommendation"
    - skills/insurance/sub-skills/health.md contains CLAUDE_SKILL_DIR/references/germany/health-insurance.md
    - skills/insurance/sub-skills/health.md contains "finyx-insurance-calc-agent" and "finyx-insurance-research-agent"
  </acceptance_criteria>
  <done>Router SKILL.md is a thin dispatcher under 100 lines. Health sub-skill contains the full health insurance flow verbatim. No health logic in the router. No YAML frontmatter or execution_context in the sub-skill. All agent and reference paths preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify insurance routing works end-to-end</name>
  <files>skills/insurance/SKILL.md, skills/insurance/sub-skills/health.md</files>
  <action>Human verifies the router dispatches correctly to the health sub-skill by running the insurance command with various inputs.</action>
  <what-built>Insurance skill converted from monolithic health advisor to router + sub-skill architecture. The /finyx:insurance command should work identically to before -- routing to health sub-skill transparently.</what-built>
  <how-to-verify>
    1. Run /finyx:insurance health -- should route to health sub-skill and begin the health insurance flow (preferences questionnaire)
    2. Run /finyx:insurance krankenversicherung -- same result (German keyword detection)
    3. Run /finyx:insurance with no arguments -- should prompt with AskUserQuestion asking which type (only Health insurance option for now)
    4. Run /finyx:insurance xyz -- should prompt for type selection (unrecognized keyword)
    5. Verify the health flow is identical: preferences questions, eligibility check, questionnaire structure all match the pre-migration behavior
  </how-to-verify>
  <verify>Human confirms routing works for all 4 input scenarios above</verify>
  <done>All 4 routing scenarios produce correct behavior. Health flow is identical to pre-migration.</done>
  <resume-signal>Type "approved" if routing works correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Router SKILL.md exists at skills/insurance/SKILL.md and is under 100 lines
- Sub-skill exists at skills/insurance/sub-skills/health.md with full health flow
- No health-specific logic in the router
- No YAML frontmatter or execution_context in the sub-skill
- All CLAUDE_SKILL_DIR paths preserved in sub-skill
- All agent references preserved in sub-skill
- Router allowed-tools is superset of all sub-skill tool requirements
</verification>

<success_criteria>
- /finyx:insurance health dispatches to health sub-skill and produces identical output to pre-migration
- /finyx:insurance with no args prompts for type and dispatches correctly
- Router contains zero health-insurance-specific logic
- Architecture supports adding new sub-skill types by adding a file + keyword entry
</success_criteria>

<output>
After completion, create .planning/phases/18-router-sub-skill-migration/18-01-SUMMARY.md
</output>
