---
phase: 20-portfolio-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/insights/SKILL.md
autonomous: true
requirements: [OPT-04]

must_haves:
  truths:
    - "Insurance total monthly cost appears as 'Insurance' row in /finyx:insights allocation table"
    - "Insurance gaps appear as action items in insights recommendations when policies[] is populated"
    - "Insights silently skips insurance row when policies[] is empty or absent"
    - "CAL-05 pattern triggers on policies[] health entry with premium > 400, not old flat fields"
  artifacts:
    - path: "skills/insights/SKILL.md"
      provides: "Updated insights with insurance.policies[] integration"
      contains: "insurance.policies"
  key_links:
    - from: "skills/insights/SKILL.md"
      to: ".finyx/profile.json"
      via: "insurance.policies[] read"
      pattern: "policies.*premium_monthly"
---

<objective>
Update `/finyx:insights` to read insurance costs from the `insurance.policies[]` array (Phase 19 schema) instead of the defunct `insurance.type` / `insurance.monthly_cost` flat fields. After this plan, the insights financial health report includes an Insurance allocation row computed from the policies array, and the CAL-05 cross-advisor pattern fires correctly against the new schema.

Purpose: OPT-04 requires insurance costs to feed into the insights financial health report.
Output: `skills/insights/SKILL.md` (updated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-portfolio-analysis/20-CONTEXT.md
@.planning/phases/20-portfolio-analysis/20-RESEARCH.md

@skills/insights/SKILL.md
@skills/profile/references/profile.json

<interfaces>
<!-- Profile schema for insurance.policies[] -->
From skills/profile/references/profile.json:
```json
"insurance": {
  "policies": [
    {
      "id": "string",
      "type": "one of: health, haftpflicht, hausrat, kfz, rechtsschutz, zahnzusatz, risikoleben, reise, fahrrad, kfz-schutzbrief, mietkaution",
      "provider": "string",
      "premium_monthly": 0,
      "premium_annual": 0,
      "coverage_amount": null,
      ...
    }
  ]
}
```

Old fields that NO LONGER EXIST (removed in Phase 19):
- `insurance.type` -- GONE
- `insurance.monthly_cost` -- GONE
- `insurance.employer_share` -- GONE

The 11 known insurance type slugs for gap detection set-difference:
health, haftpflicht, hausrat, kfz, rechtsschutz, zahnzusatz, risikoleben, reise, fahrrad, kfz-schutzbrief, mietkaution

Essential types for gap action items (subset that matters for recommendations):
haftpflicht, hausrat, health (these are mandatory/essential for all residents)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update insights SKILL.md insurance integration</name>
  <files>skills/insights/SKILL.md</files>
  <read_first>
    skills/insights/SKILL.md
    skills/profile/references/profile.json
    .planning/phases/20-portfolio-analysis/20-RESEARCH.md
  </read_first>
  <action>
Edit `skills/insights/SKILL.md` in exactly 4 locations. Do NOT change any other content.

**Edit 1 — Phase 1 Optional fields block (around line 68-69)**

Replace:
```
**Optional fields — Insurance integration:**
- `insurance.type` and `insurance.monthly_cost` — if populated, insurance costs are included in allocation analysis as a "needs" line item. If absent or null: skip silently (no error, no warning).
```

With:
```
**Optional fields — Insurance integration:**
- `insurance.policies[]` — if array is non-empty, compute total monthly premium by summing `policies[].premium_monthly` across all entries. Include as "Insurance" needs line item with label "Insurance (X policies)" and monthly total. If array is empty or absent: skip silently (no error, no warning).
- **Gap action items:** Compare the `type` values present in `policies[]` against the essential types list (haftpflicht, hausrat, health). Any essential type NOT represented in policies[] is surfaced as a recommendation action item: "Missing: [type1], [type2]". This is a simple set-difference check — it does NOT require spawning the portfolio agent.
```

**Edit 2 — Phase 3 Task 1 Allocation Agent prompt (around line 138-142)**

Replace the insurance block in the allocation agent Task prompt:
```
If `.finyx/profile.json` contains an `insurance` section with `type` and `monthly_cost` populated:
- Include the net insurance cost (monthly_cost minus employer_share) as a "needs" category line item labeled "Health Insurance ([type])"
- If insurance.type is "PKV" and the net cost exceeds what GKV would cost at the user's income level, flag this as a recommendation opportunity: "PKV premium exceeds estimated GKV equivalent — review /finyx:insurance for cost comparison"
- If insurance section is absent, null, or type is "none": skip — do not include any insurance line in allocation
```

With:
```
If `.finyx/profile.json` contains a non-empty `insurance.policies[]` array:
- Sum all `policies[].premium_monthly` values to compute total monthly insurance cost
- Include as a "needs" category line item labeled "Insurance ([N] policies — EUR [total]/month)" where N = count of policies and total = computed sum
- If any policy has premium_monthly of 0 or null, note "[DATA GAP: premium_monthly missing for [policy.id]]" inline
- Compare policies[].type values against essential types (haftpflicht, hausrat, health). For each essential type NOT in policies[]: add to recommendation: "Missing insurance: [type] — run /finyx:insurance for details"
- Do NOT reference insurance.type or insurance.monthly_cost — those fields do not exist in the schema
- If insurance.policies[] is absent or empty: skip — do not include any insurance line in allocation
```

**Edit 3 — Phase 5 CAL-05 pattern (around line 278-280)**

Replace:
```
- Trigger: `insurance.type == "PKV"` AND net insurance cost (monthly_cost - employer_share) exceeds €400/month AND savings rate is below 20%
```

With:
```
- Trigger: A `health` type policy exists in `insurance.policies[]` with `premium_monthly > 400` AND savings rate is below 20%
```

**Edit 4 — Notes section Cross-Skill Data Dependencies (around line 399-400)**

Replace:
```
- **Insurance skill data:** `insurance.type`, `insurance.monthly_cost`, `insurance.employer_share` — used for allocation and cross-advisor patterns
```

With:
```
- **Insurance skill data:** `insurance.policies[]` array — sum of `premium_monthly` used for allocation; `type` field used for gap detection and CAL-05 pattern
```
  </action>
  <verify>
    <automated>grep -q "insurance.policies\[\]" skills/insights/SKILL.md && grep -q "premium_monthly" skills/insights/SKILL.md && ! grep -q "insurance.monthly_cost" skills/insights/SKILL.md && ! grep -q 'insurance.type ==' skills/insights/SKILL.md && grep -q "Missing insurance" skills/insights/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 1 optional fields references `insurance.policies[]` not `insurance.type`/`insurance.monthly_cost`
    - Phase 1 includes gap action items logic: set-difference of policies[].type vs essential types
    - Phase 3 allocation agent prompt sums `policies[].premium_monthly` and labels "Insurance ([N] policies)"
    - Phase 3 allocation agent prompt does NOT reference `insurance.type`, `insurance.monthly_cost`, or `insurance.employer_share`
    - Phase 3 allocation agent prompt includes `[DATA GAP]` handling for missing premium_monthly
    - Phase 3 allocation agent prompt includes missing essential types recommendation
    - CAL-05 trigger reads `insurance.policies[]` for health type with `premium_monthly > 400`
    - Notes Cross-Skill Data Dependencies references `insurance.policies[]` array
    - No references to old flat fields `insurance.type`, `insurance.monthly_cost`, `insurance.employer_share` remain anywhere in the file
    - All other content unchanged (tax scoring, projection, disclaimer, allocation mapping, etc.)
  </acceptance_criteria>
  <done>Insights SKILL.md reads insurance costs from policies[] array, shows Insurance allocation row, surfaces missing essential types as action items, CAL-05 fires on policies[] health entry</done>
</task>

</tasks>

<verification>
1. `grep -c "insurance.policies" skills/insights/SKILL.md` — returns >= 4 (Phase 1, Phase 3, CAL-05, Notes)
2. `! grep -q "insurance.monthly_cost" skills/insights/SKILL.md` — old field reference fully removed
3. `! grep -q "insurance.employer_share" skills/insights/SKILL.md` — old field reference fully removed
4. `grep -q "premium_monthly" skills/insights/SKILL.md` — new field referenced
5. `grep -q "Missing insurance" skills/insights/SKILL.md` — gap action items present
</verification>

<success_criteria>
- Insights SKILL.md computes insurance allocation from `insurance.policies[].premium_monthly` sum
- Old flat fields (`insurance.type`, `insurance.monthly_cost`) are completely removed
- Gap detection uses set-difference against essential types (no portfolio agent spawn needed)
- CAL-05 cross-advisor pattern fires on policies[] health entry with premium_monthly > 400
- OPT-04 is fully addressed: insurance costs feed into the financial health report
</success_criteria>

<output>
After completion, create `.planning/phases/20-portfolio-analysis/20-02-SUMMARY.md`
</output>
