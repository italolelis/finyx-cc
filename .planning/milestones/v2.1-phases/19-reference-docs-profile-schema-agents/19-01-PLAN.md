---
phase: 19-reference-docs-profile-schema-agents
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/insurance/references/disclaimer.md
  - skills/profile/references/profile.json
  - skills/insurance/SKILL.md
  - skills/insurance/references/germany/health-insurance.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "disclaimer.md contains section 34d GewO advisory-only notice"
    - "profile.json has insurance.policies[] array with all required fields"
    - "SKILL.md keyword map covers all 11 insurance types with German aliases"
    - "health-insurance.md has Field Extraction Schema section matching profile.json field names"
  artifacts:
    - path: "skills/insurance/references/disclaimer.md"
      provides: "section 34d GewO insurance advisory notice"
      contains: "34d GewO"
    - path: "skills/profile/references/profile.json"
      provides: "insurance.policies[] schema"
      contains: "insurance"
    - path: "skills/insurance/SKILL.md"
      provides: "Keyword map for all 11 insurance types"
      contains: "haftpflicht"
    - path: "skills/insurance/references/germany/health-insurance.md"
      provides: "Field Extraction Schema section"
      contains: "Field Extraction Schema"
  key_links:
    - from: "skills/profile/references/profile.json"
      to: "skills/insurance/references/germany/health-insurance.md"
      via: "Field names in extraction schema must match profile.json insurance.policies[] fields"
      pattern: "premium_monthly|premium_annual|coverage_amount|kuendigungsfrist_months"
---

<objective>
Create the foundational infrastructure contracts that all subsequent reference docs and agents depend on: the section 34d GewO disclaimer update, the insurance.policies[] profile schema extension, the SKILL.md keyword map expansion for all 11 types, and the Field Extraction Schema addition to the existing health-insurance.md.

Purpose: These four artifacts define the data contracts and legal compliance baseline that every reference doc (Plan 02) and agent (Plan 03) will consume. They must exist first so downstream artifacts can reference concrete field names and legal language.

Output: Updated disclaimer.md, profile.json, SKILL.md, and health-insurance.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reference-docs-profile-schema-agents/19-RESEARCH.md
@skills/insurance/references/disclaimer.md
@skills/profile/references/profile.json
@skills/insurance/SKILL.md
@skills/insurance/references/germany/health-insurance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update disclaimer, profile schema, and SKILL.md keyword map</name>
  <files>
    skills/insurance/references/disclaimer.md
    skills/profile/references/profile.json
    skills/insurance/SKILL.md
  </files>
  <read_first>
    skills/insurance/references/disclaimer.md
    skills/profile/references/profile.json
    skills/insurance/SKILL.md
    .planning/phases/19-reference-docs-profile-schema-agents/19-RESEARCH.md
  </read_first>
  <action>
**1. Update disclaimer.md (INFRA-02, INFRA-03):**

Add the following block BEFORE the existing "You should:" list in disclaimer.md:

```
**Insurance advisory notice (section 34d GewO):** Finyx is not a licensed insurance broker or Versicherungsmakler under section 34d GewO. All insurance guidance is educational and criteria-based only. Finyx does not recommend specific insurance products, tariffs, or providers. For binding advice and product selection, consult a licensed Versicherungsberater or Versicherungsmakler.
```

Keep all existing content intact. The section 34d notice is additive.

**2. Extend profile.json (INFRA-01):**

Add an `"insurance"` top-level key to `skills/profile/references/profile.json` after the `"pension"` block. Use this exact structure:

```json
"insurance": {
  "policies": [
    {
      "id": "string — slug like haftpflicht-huk-2024",
      "type": "one of: health, haftpflicht, hausrat, kfz, rechtsschutz, zahnzusatz, risikoleben, reise, fahrrad, kfz-schutzbrief, mietkaution",
      "provider": "string",
      "premium_monthly": 0,
      "premium_annual": 0,
      "coverage_amount": null,
      "start_date": "ISO_DATE",
      "renewal_date": null,
      "kuendigungsfrist_months": 3,
      "sonderkundigungsrecht": false,
      "doc_path": null,
      "coverage_components": [],
      "notes": null,
      "last_updated": "ISO_DATE"
    }
  ]
}
```

Field notes as JSON comments are not possible — add an `"_insurance_schema"` description block (same pattern as existing `"_holdings_schema"`) documenting:
- `type` is constrained to the 11 known slugs
- `premium_monthly` and `premium_annual` are separate (annual discount common)
- `coverage_amount` is null for service-based types (rechtsschutz, reise)
- `doc_path` is relative from project root (e.g., `.finyx/docs/hausrat-policy.pdf`)
- `coverage_components` is string[] extracted by document reader agent

**3. Expand SKILL.md keyword map (all 11 types):**

In SKILL.md Phase 0, expand the keyword map to include all 11 types. Add these entries after the existing health entry:

```
- "haftpflicht", "liability", "privathaftpflicht", "haftpflichtversicherung" → sub-skill: haftpflicht
- "hausrat", "household", "contents", "hausratversicherung" → sub-skill: hausrat
- "kfz", "car", "auto", "kfz-versicherung", "autoversicherung", "kasko" → sub-skill: kfz
- "rechtsschutz", "legal", "rechtsschutzversicherung" → sub-skill: rechtsschutz
- "zahnzusatz", "dental", "zahn", "zahnzusatzversicherung" → sub-skill: zahnzusatz
- "risikoleben", "life", "term life", "risikolebensversicherung" → sub-skill: risikoleben
- "reise", "travel", "reiseversicherung" → sub-skill: reise
- "fahrrad", "bicycle", "bike", "fahrradversicherung" → sub-skill: fahrrad
- "kfz-schutzbrief", "schutzbrief", "roadside", "schutzbriefversicherung" → sub-skill: kfz-schutzbrief
- "mietkaution", "rental deposit", "kaution", "mietkautionsversicherung" → sub-skill: mietkaution
```

Also update the AskUserQuestion singleSelect options to list all 11 types (not just health). Use German name + English description for each option, matching the health pattern.

Update the error_handling section to list all 11 types as available.

Update the notes section — the tool requirements note should say "Current sub-skill tool requirements:" followed by health.md's tools (existing) and a placeholder note: "Sub-skills for other types will be added in Phases 21-22."
  </action>
  <verify>
    <automated>cd /Users/italovietro/projects/immo && grep -c "34d GewO" skills/insurance/references/disclaimer.md && grep -c '"insurance"' skills/profile/references/profile.json && grep -c "haftpflicht" skills/insurance/SKILL.md && grep -c "mietkaution" skills/insurance/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "34d GewO" skills/insurance/references/disclaimer.md returns at least 1 match
    - grep '"insurance"' skills/profile/references/profile.json returns at least 1 match
    - grep "premium_monthly" skills/profile/references/profile.json returns at least 1 match
    - grep "policies" skills/profile/references/profile.json returns at least 1 match
    - grep "haftpflicht" skills/insurance/SKILL.md returns at least 1 match
    - grep "mietkaution" skills/insurance/SKILL.md returns at least 1 match
    - grep "zahnzusatz" skills/insurance/SKILL.md returns at least 1 match
    - All 11 types appear in the AskUserQuestion singleSelect list
  </acceptance_criteria>
  <done>disclaimer.md has section 34d GewO notice, profile.json has insurance.policies[] with all fields, SKILL.md routes all 11 types</done>
</task>

<task type="auto">
  <name>Task 2: Add Field Extraction Schema to health-insurance.md</name>
  <files>
    skills/insurance/references/germany/health-insurance.md
  </files>
  <read_first>
    skills/insurance/references/germany/health-insurance.md
    skills/profile/references/profile.json
    .planning/phases/19-reference-docs-profile-schema-agents/19-RESEARCH.md
  </read_first>
  <action>
Add a new section `## Field Extraction Schema` at the end of health-insurance.md (before the final source line). This section enables the document reader agent (Phase 19, Plan 03) to extract structured data from health insurance policy PDFs.

The field names MUST match the `insurance.policies[]` fields in `skills/profile/references/profile.json` exactly (read profile.json first to verify field names).

Add this content:

```markdown
## Field Extraction Schema

The document reader agent extracts these fields from health insurance policy PDFs. Field names match `insurance.policies[]` in `.finyx/profile.json`.

| Field | German Label in Policy | Type | Notes |
|-------|----------------------|------|-------|
| provider | Versicherer / Krankenversicherung | string | Full company name (e.g., "Debeka Krankenversicherungsverein a.G.") |
| premium_monthly | Monatsbeitrag / Monatliche Prämie | number | EUR; for PKV convert from annual if only Jahresbeitrag stated |
| premium_annual | Jahresbeitrag / Jahresprämie | number | EUR; for GKV may need to compute from monthly |
| coverage_amount | Versicherungssumme | number/null | null for GKV (unlimited statutory coverage); for PKV set to null (service-based, not sum-based) |
| start_date | Versicherungsbeginn | ISO date | Date coverage started |
| renewal_date | Hauptfälligkeit / Verlängerungsdatum | ISO date | Annual renewal date |
| kuendigungsfrist_months | Kündigungsfrist | number | PKV: typically 3 months; GKV: immediate for PKV-eligible earners |
| coverage_components | Leistungsumfang / Versicherungsschutz | string[] | For PKV: tariff tier names and coverage areas (ambulant, stationär, Zahn). For GKV: "Gesetzlicher Leistungskatalog" |
| exclusions | Ausschlüsse / Nicht versichert | string[] | PKV-specific: pre-existing condition exclusions, waiting period items |

### Health-Specific Extraction Notes

- GKV policies do not have a traditional "policy document" — extract from Mitgliedsbescheinigung or Beitragsbescheid
- PKV policies: look for Versicherungsschein (insurance certificate) as primary extraction source
- Tariff name (e.g., "AM TOP", "EL Bonus") should be extracted into `notes` field
- Selbstbeteiligung level should be extracted into `notes` field (e.g., "SB EUR 600/year")
- Beitragsrückerstattung eligibility should be noted in `notes` field
```

Do NOT modify any existing sections of health-insurance.md. This is purely additive.
  </action>
  <verify>
    <automated>cd /Users/italovietro/projects/immo && grep -c "Field Extraction Schema" skills/insurance/references/germany/health-insurance.md && grep -c "premium_monthly" skills/insurance/references/germany/health-insurance.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Field Extraction Schema" skills/insurance/references/germany/health-insurance.md returns at least 1 match
    - grep "premium_monthly" skills/insurance/references/germany/health-insurance.md returns at least 1 match
    - grep "coverage_components" skills/insurance/references/germany/health-insurance.md returns at least 1 match
    - grep "kuendigungsfrist_months" skills/insurance/references/germany/health-insurance.md returns at least 1 match
    - All existing sections (1-6) of health-insurance.md are preserved unchanged
  </acceptance_criteria>
  <done>health-insurance.md has Field Extraction Schema section with field names matching profile.json insurance.policies[] exactly</done>
</task>

</tasks>

<verification>
- All four files modified: disclaimer.md, profile.json, SKILL.md, health-insurance.md
- section 34d GewO language present in disclaimer.md
- insurance.policies[] array with all 14 fields present in profile.json
- All 11 insurance type keywords mapped in SKILL.md
- Field Extraction Schema in health-insurance.md uses exact same field names as profile.json
</verification>

<success_criteria>
- disclaimer.md includes section 34d GewO advisory-only notice (INFRA-02)
- profile.json has insurance.policies[] with type enum, premium fields, coverage fields, doc_path (INFRA-01)
- SKILL.md keyword map covers health, haftpflicht, hausrat, kfz, rechtsschutz, zahnzusatz, risikoleben, reise, fahrrad, kfz-schutzbrief, mietkaution
- health-insurance.md Field Extraction Schema field names match profile.json exactly
- Criteria-based language constraint documented in disclaimer (INFRA-03)
</success_criteria>

<output>
After completion, create `.planning/phases/19-reference-docs-profile-schema-agents/19-01-SUMMARY.md`
</output>
