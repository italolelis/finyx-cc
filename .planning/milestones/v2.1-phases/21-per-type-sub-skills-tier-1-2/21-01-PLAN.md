---
phase: 21-per-type-sub-skills-tier-1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/insurance/sub-skills/haftpflicht.md
  - skills/insurance/sub-skills/hausrat.md
  - skills/insurance/sub-skills/rechtsschutz.md
autonomous: true
requirements: [TYPE-01, TYPE-02, TYPE-04, OPT-01, OPT-03]

must_haves:
  truths:
    - "User can get Privathaftpflicht analysis with coverage vs €5M+ benchmark and PASS/FAIL table"
    - "User can get Hausrat analysis with living-area-based sum benchmark (€650/m²)"
    - "User can get Rechtsschutz analysis with module-based coverage comparison (not monetary sum)"
    - "Each sub-skill spawns the research agent for criteria-based market comparison"
    - "Each sub-skill shows cancellation deadline or 'Unknown' fallback"
  artifacts:
    - path: "skills/insurance/sub-skills/haftpflicht.md"
      provides: "Privathaftpflicht advisory sub-skill"
      contains: "# Privathaftpflicht Insurance Sub-skill"
    - path: "skills/insurance/sub-skills/hausrat.md"
      provides: "Hausrat advisory sub-skill"
      contains: "# Hausrat Insurance Sub-skill"
    - path: "skills/insurance/sub-skills/rechtsschutz.md"
      provides: "Rechtsschutz advisory sub-skill"
      contains: "# Rechtsschutz Insurance Sub-skill"
  key_links:
    - from: "skills/insurance/sub-skills/haftpflicht.md"
      to: "skills/insurance/agents/finyx-insurance-research-agent.md"
      via: "Task tool spawn with insurance_type: haftpflicht"
      pattern: "insurance_type.*haftpflicht"
    - from: "skills/insurance/sub-skills/hausrat.md"
      to: "skills/insurance/references/germany/hausrat.md"
      via: "Read for coverage benchmarks"
      pattern: "references/germany/hausrat.md"
    - from: "skills/insurance/sub-skills/rechtsschutz.md"
      to: "insurance.policies[]"
      via: "coverage_components for module list"
      pattern: "coverage_components"
---

<objective>
Create 3 per-type insurance sub-skills: Privathaftpflicht (TYPE-01), Hausrat (TYPE-02), and Rechtsschutz (TYPE-04). Each follows health.md's multi-phase pattern with preferences > validation > disclaimer > benchmark comparison > cancellation check > research agent spawn > recommendation. All are read-only advisory, no file writes.

Purpose: Enable users to get detailed, type-specific insurance analysis for the three simpler insurance types.
Output: 3 new sub-skill Markdown files under skills/insurance/sub-skills/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-type-sub-skills-tier-1-2/21-CONTEXT.md
@.planning/phases/21-per-type-sub-skills-tier-1-2/21-RESEARCH.md

@skills/insurance/sub-skills/health.md
@skills/insurance/sub-skills/portfolio.md
@skills/insurance/SKILL.md
@skills/insurance/agents/finyx-insurance-research-agent.md
@skills/insurance/references/germany/haftpflicht.md
@skills/insurance/references/germany/hausrat.md
@skills/insurance/references/germany/rechtsschutz.md

<interfaces>
<!-- Sub-skill file contract (from SKILL.md and existing sub-skills): -->
- No YAML frontmatter
- No <execution_context> block (router loads disclaimer.md and profile.json)
- Start with `# [Type] Insurance Sub-skill`
- Comment: `<!-- Sub-skill loaded by router SKILL.md. All CLAUDE_SKILL_DIR paths resolve to skills/insurance/ -->`
- Sections: <objective>, <process> (Phase 0-6), <error_handling>, <notes>
- Router dispatches to: sub-skills/haftpflicht.md, sub-skills/hausrat.md, sub-skills/rechtsschutz.md

<!-- Profile policy schema (from portfolio.md Phase 0): -->
- insurance.policies[] entries have: type (slug), provider, premium_monthly, coverage_amount, coverage_components[], start_date, renewal_date, kuendigungsfrist_months, sonderkundigungsrecht (boolean), notes

<!-- Research agent Task spawn format: -->
- Task tool to finyx-insurance-research-agent
- Required param: insurance_type (slug)
- Optional params: current_premium_monthly, user preferences
- Output wrapped in <insurance_research_result> tags
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Privathaftpflicht sub-skill (haftpflicht.md)</name>
  <files>skills/insurance/sub-skills/haftpflicht.md</files>
  <read_first>
    skills/insurance/sub-skills/health.md
    skills/insurance/sub-skills/portfolio.md
    skills/insurance/references/germany/haftpflicht.md
    skills/insurance/SKILL.md
  </read_first>
  <action>
Create `skills/insurance/sub-skills/haftpflicht.md` — a plain Markdown sub-skill file (~150-180 lines) following health.md's structure but simplified (no JAEG gate, no parallel agents, no health flags).

**File structure:**
1. `# Privathaftpflicht Insurance Sub-skill` heading
2. HTML comment: `<!-- Sub-skill loaded by router SKILL.md. All CLAUDE_SKILL_DIR paths resolve to skills/insurance/ -->`
3. `<objective>` — Deliver personalized Privathaftpflicht analysis: coverage benchmark comparison, cancellation tracking, criteria-based market research. Read-only, writes NO files.
4. `<process>` with 7 phases:

**Phase 0: Preferences** — Use AskUserQuestion to collect:
- Q1: Budget range (singleSelect): "Under €5/month", "€5–10/month", "€10–15/month", "€15+/month", "No budget constraint"
- Q2: Are you renting? (singleSelect): "Yes — renting", "No — homeowner"
- Q3: Do you have pets? (singleSelect): "No pets", "Dog(s)", "Cat(s) only", "Other pets"
Store as `user_preferences` object. Pass to research agent in Phase 5.

**Phase 1: Validation and Profile Read** — Check profile exists (`[ -f .finyx/profile.json ]`). Read `insurance.policies[]` to find entry where `type == "haftpflicht"`. Extract `coverage_amount`, `premium_monthly`, `coverage_components`, `start_date`, `renewal_date`, `kuendigungsfrist_months`, `sonderkundigungsrecht`. Set `existing_policy_found = true/false`. If profile missing: error message directing to `/finyx:profile`.

**Phase 2: Disclaimer** — Emit disclaimer banner. Pattern from portfolio.md Phase 1:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 FINYX ► INSURANCE ► PRIVATHAFTPFLICHT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
Then emit full disclaimer.md content (already loaded by router). Add insurance-specific addendum: "This analysis evaluates coverage criteria. It does not constitute a recommendation for any specific insurance product or provider."

**Phase 3: Coverage Benchmark Comparison** — Read `${CLAUDE_SKILL_DIR}/references/germany/haftpflicht.md` Coverage Benchmarks section. Build comparison table:

If `existing_policy_found`:
```
| Criterion | Your Coverage | Recommended Minimum | Status |
|-----------|--------------|---------------------|--------|
| Deckungssumme | €{coverage_amount} | ≥€5,000,000 | PASS/FAIL |
| Mietsachschaden | {from coverage_components} | Included | PASS/FAIL |
| Schlüsselverlust | {from coverage_components} | Included | PASS/FAIL |
| Gefälligkeitsschäden | {from coverage_components} | Included | PASS/FAIL |
| Forderungsausfalldeckung | {from coverage_components} | Included | PASS/FAIL |
| Selbstbeteiligung | {from notes or coverage_components} | €0–150 | INFO |
```
PASS/FAIL logic: Read thresholds from reference doc at runtime — do NOT hardcode values. Compare user's `coverage_amount` against reference doc's "Minimum Acceptable" column.

If NOT `existing_policy_found`: Show benchmarks only with note: "No Haftpflicht policy found in your profile. Add your policy via `/finyx:insurance portfolio` to see a personalized comparison."

**Phase 4: Cancellation Deadline Check** — If `existing_policy_found` AND `start_date` is present:
- Compute Hauptfälligkeit: use `renewal_date` if present, else compute next anniversary from `start_date`
- Compute deadline: `Hauptfälligkeit - kuendigungsfrist_months months`
- Compute `days_until_deadline = deadline - today`
- Cases: passed → show next opportunity; 0-30 days → ALERT banner; >30 days → show date informatively
- If `sonderkundigungsrecht == true`: show Sonderkündigungsrecht alert banner
- Use exact banner formats from 21-RESEARCH.md Code Examples section

If cancellation fields missing: show "UNKNOWN" fallback banner directing to `/finyx:insurance portfolio`.

If NOT `existing_policy_found`: skip phase with brief note.

**Phase 5: Research Agent Spawn** — Spawn `finyx-insurance-research-agent` via Task tool:
```
insurance_type: haftpflicht
current_premium_monthly: {from profile if known, else omit}
<user_preferences>
budget_range: {selected}
renting: {yes/no}
pets: {type}
</user_preferences>
```
Render the `<insurance_research_result>` output to the user.

**Phase 6: Recommendation** — Synthesize: (a) coverage gaps from Phase 3, (b) cancellation window from Phase 4, (c) market context from Phase 5 into 2-3 paragraph advisory. Include concrete next steps (e.g., "Consider increasing Deckungssumme to €10M+" if FAIL). If renting and Mietsachschaden missing: flag explicitly.

5. `<error_handling>` — Profile missing error, reference doc read error, research agent Task failure fallback.
6. `<notes>` — Haftpflicht is simplest type. Hundehaftpflicht is a separate policy — if user has dogs, note that Privathaftpflicht does NOT cover dog liability. Reference doc loading note (router loads disclaimer.md and profile.json; sub-skill reads haftpflicht.md reference via Read tool at Phase 3). Sub-skill writes NO files.

**CRITICAL anti-patterns to avoid:**
- Do NOT add YAML frontmatter
- Do NOT add `<execution_context>` block
- Do NOT write to profile.json
- Do NOT hardcode benchmark values — read from reference doc
- Do NOT name specific providers or tariffs (§34d GewO)
- Do NOT spawn parallel agents (only one research agent)
  </action>
  <verify>
    <automated>test -f skills/insurance/sub-skills/haftpflicht.md && grep -q "# Privathaftpflicht Insurance Sub-skill" skills/insurance/sub-skills/haftpflicht.md && grep -q "insurance_type.*haftpflicht" skills/insurance/sub-skills/haftpflicht.md && grep -q "Phase 0: Preferences" skills/insurance/sub-skills/haftpflicht.md && grep -q "Phase 4: Cancellation" skills/insurance/sub-skills/haftpflicht.md && grep -q "finyx-insurance-research-agent" skills/insurance/sub-skills/haftpflicht.md && grep -q "coverage_amount" skills/insurance/sub-skills/haftpflicht.md && grep -q "sonderkundigungsrecht" skills/insurance/sub-skills/haftpflicht.md && ! grep -q "^---" skills/insurance/sub-skills/haftpflicht.md && ! grep -q "execution_context" skills/insurance/sub-skills/haftpflicht.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `# Privathaftpflicht Insurance Sub-skill` (no YAML frontmatter)
    - Contains `<!-- Sub-skill loaded by router SKILL.md` comment
    - Has all 7 phases: Phase 0 (Preferences), Phase 1 (Validation), Phase 2 (Disclaimer), Phase 3 (Coverage Benchmark), Phase 4 (Cancellation), Phase 5 (Research Agent), Phase 6 (Recommendation)
    - Phase 3 reads benchmarks from `references/germany/haftpflicht.md` — grep for `CLAUDE_SKILL_DIR.*references/germany/haftpflicht`
    - Phase 4 uses `start_date` (NOT `vertrag_beginn`), `kuendigungsfrist_months`, `sonderkundigungsrecht`
    - Phase 5 spawns `finyx-insurance-research-agent` with `insurance_type: haftpflicht`
    - No `<execution_context>` block anywhere in file
    - No YAML frontmatter (`---` block at top)
    - No Write tool calls or profile.json writes
    - File is 140-200 lines
  </acceptance_criteria>
  <done>haftpflicht.md exists, follows sub-skill contract, has all 7 phases with benchmark comparison, cancellation tracking, and research agent spawn</done>
</task>

<task type="auto">
  <name>Task 2: Create Hausrat sub-skill (hausrat.md)</name>
  <files>skills/insurance/sub-skills/hausrat.md</files>
  <read_first>
    skills/insurance/sub-skills/health.md
    skills/insurance/sub-skills/haftpflicht.md
    skills/insurance/references/germany/hausrat.md
  </read_first>
  <action>
Create `skills/insurance/sub-skills/hausrat.md` — a plain Markdown sub-skill file (~160-200 lines) following the same structure as haftpflicht.md but with Hausrat-specific logic.

**Key differences from haftpflicht:**

**Phase 0: Preferences** — Use AskUserQuestion:
- Q1: Living area in m² (text input): "What is your apartment/house living area in square meters (Wohnfläche)?" — needed for Versicherungssumme benchmark
- Q2: Elementarschäden interest (singleSelect): "Are you interested in Elementarschäden coverage (flooding, earthquake, landslide)?" — "Yes, important to me" / "No, not needed" / "Not sure — include in analysis"
- Q3: Fahrrad-Zusatz interest (singleSelect): "Do you own valuable bicycles you'd like covered?" — "Yes — have bikes worth >€500" / "No" / "Not sure"
Store as `user_preferences`. The `living_area_sqm` value from Q1 is critical for Phase 3.

**Phase 1: Validation and Profile Read** — Same pattern as haftpflicht but type slug is `"hausrat"`. Additionally check for `livingAreaSqm` in profile (try `investor.livingAreaSqm`, `criteria.minSize`, or any sqm-related field) as fallback if user didn't provide in Phase 0.

**Phase 3: Coverage Benchmark Comparison** — THE KEY DIFFERENCE: Hausrat benchmark is computed, not static.
- `minimum_sum = living_area_sqm × 650` (€650/m² from reference doc)
- Read Unterversicherungsverzicht requirement from reference doc

Comparison table (if existing policy found):
```
| Criterion | Your Coverage | Recommended Minimum | Status |
|-----------|--------------|---------------------|--------|
| Versicherungssumme | €{coverage_amount} | ≥€{living_area × 650} | PASS/FAIL |
| Unterversicherungsverzicht | {from coverage_components} | Included | PASS/FAIL |
| Elementarschäden | {from coverage_components} | Recommended in risk areas | INFO |
| Einbruchdiebstahl | {from coverage_components} | Included | PASS/FAIL |
```

If `living_area_sqm` is unknown (neither from user preferences Q1 nor profile): show benchmark as "UNKNOWN — provide living area for personalized assessment" and mark adequacy as UNKNOWN. Do NOT skip Phase 3.

**Phase 4: Cancellation** — Same logic as haftpflicht. Additional note: address change (Umzug) triggers Sonderkündigungsrecht if premium increases — mention this in the cancellation section.

**Phase 5: Research Agent** — `insurance_type: hausrat`. Pass `living_area_sqm` and Elementarschäden preference as additional context.

**Phase 6: Recommendation** — Synthesize. If Unterversicherungsverzicht is missing, flag prominently (this means partial coverage payouts). If living area × 650 > coverage_amount, show the gap in EUR.

Same anti-patterns apply: no frontmatter, no execution_context, no writes, no hardcoded benchmarks, no provider names.
  </action>
  <verify>
    <automated>test -f skills/insurance/sub-skills/hausrat.md && grep -q "# Hausrat Insurance Sub-skill" skills/insurance/sub-skills/hausrat.md && grep -q "insurance_type.*hausrat" skills/insurance/sub-skills/hausrat.md && grep -q "650" skills/insurance/sub-skills/hausrat.md && grep -q "Unterversicherungsverzicht" skills/insurance/sub-skills/hausrat.md && grep -q "living_area" skills/insurance/sub-skills/hausrat.md && grep -q "Elementar" skills/insurance/sub-skills/hausrat.md && grep -q "sonderkundigungsrecht" skills/insurance/sub-skills/hausrat.md && ! grep -q "^---" skills/insurance/sub-skills/hausrat.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `# Hausrat Insurance Sub-skill`
    - Phase 0 collects living area in m² from user
    - Phase 3 computes benchmark as `living_area × 650` — grep for `650`
    - Phase 3 checks Unterversicherungsverzicht — grep for `Unterversicherungsverzicht`
    - Phase 3 handles missing living area gracefully (UNKNOWN, not crash/skip)
    - Phase 4 mentions Umzug/address change as Sonderkündigungsrecht trigger
    - Phase 5 spawns research agent with `insurance_type: hausrat`
    - No YAML frontmatter, no execution_context, no file writes
    - File is 150-220 lines
  </acceptance_criteria>
  <done>hausrat.md exists with computed living-area benchmark, Unterversicherungsverzicht check, Elementarschäden coverage, and cancellation tracking including Umzug trigger</done>
</task>

<task type="auto">
  <name>Task 3: Create Rechtsschutz sub-skill (rechtsschutz.md)</name>
  <files>skills/insurance/sub-skills/rechtsschutz.md</files>
  <read_first>
    skills/insurance/sub-skills/haftpflicht.md
    skills/insurance/references/germany/rechtsschutz.md
  </read_first>
  <action>
Create `skills/insurance/sub-skills/rechtsschutz.md` — a plain Markdown sub-skill file (~160-200 lines) following the same structure but with module-based coverage logic (not monetary sum).

**Key differences from haftpflicht/hausrat:**

**Phase 0: Preferences** — Use AskUserQuestion:
- Q1: Employment status (singleSelect): "Employee", "Self-employed", "Civil servant (Beamter)", "Retired", "Student"
- Q2: Do you rent your home? (singleSelect): "Yes — renting" / "No — homeowner"
- Q3: Do you drive a car? (singleSelect): "Yes — regular driver" / "No"
These determine which Rechtsschutz modules are relevant: Berufsrechtsschutz (employee/self-employed), Mietrechtsschutz (renter), Verkehrsrechtsschutz (driver), Privatrechtsschutz (always).

**Phase 1: Validation and Profile Read** — Type slug `"rechtsschutz"`. Extract `coverage_components` — this contains active modules (e.g., `["Berufsrechtsschutz", "Verkehrsrechtsschutz"]`). CRITICAL: `coverage_amount` is null for Rechtsschutz (service_based) — NEVER show a monetary sum benchmark.

**Phase 3: Coverage Benchmark Comparison** — MODULE-BASED comparison, not sum-based:
```
| Module | Your Coverage | Recommended | Status |
|--------|--------------|-------------|--------|
| Privatrechtsschutz | {in coverage_components?} | Always recommended | PASS/MISSING |
| Berufsrechtsschutz | {in coverage_components?} | Recommended if employed | PASS/MISSING/N/A |
| Verkehrsrechtsschutz | {in coverage_components?} | Recommended if driving | PASS/MISSING/N/A |
| Mietrechtsschutz | {in coverage_components?} | Recommended if renting | PASS/MISSING/N/A |
| Wartezeit status | {compute from start_date} | 3 months (standard) | INFO |
| Selbstbeteiligung | {from notes/components} | €150–300 typical | INFO |
```

N/A = module not relevant based on user's preferences (e.g., Verkehrsrechtsschutz N/A if user doesn't drive). Wartezeit: if policy `start_date` is within last 3 months, note "Wartezeit may still be active — coverage for pre-existing legal matters may be limited."

**Phase 4: Cancellation** — Same logic. Note: ongoing covered legal cases are NOT voided by cancellation.

**Phase 5: Research Agent** — `insurance_type: rechtsschutz`. Pass employment status and relevant modules as context.

**Phase 6: Recommendation** — Focus on missing modules vs user's life situation. If user is renting without Mietrechtsschutz, flag it. If driving without Verkehrsrechtsschutz, flag it.

Same anti-patterns. ADDITIONALLY: never show coverage_amount as a monetary benchmark for Rechtsschutz — it's always null/service-based.
  </action>
  <verify>
    <automated>test -f skills/insurance/sub-skills/rechtsschutz.md && grep -q "# Rechtsschutz Insurance Sub-skill" skills/insurance/sub-skills/rechtsschutz.md && grep -q "insurance_type.*rechtsschutz" skills/insurance/sub-skills/rechtsschutz.md && grep -q "coverage_components" skills/insurance/sub-skills/rechtsschutz.md && grep -q "Berufsrechtsschutz" skills/insurance/sub-skills/rechtsschutz.md && grep -q "Verkehrsrechtsschutz" skills/insurance/sub-skills/rechtsschutz.md && grep -q "Mietrechtsschutz" skills/insurance/sub-skills/rechtsschutz.md && grep -q "Wartezeit" skills/insurance/sub-skills/rechtsschutz.md && ! grep -q "^---" skills/insurance/sub-skills/rechtsschutz.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `# Rechtsschutz Insurance Sub-skill`
    - Phase 3 uses MODULE-BASED comparison (Berufsrechtsschutz, Verkehrsrechtsschutz, Mietrechtsschutz, Privatrechtsschutz)
    - Phase 3 does NOT show monetary coverage_amount benchmark (it's null for service-based types)
    - Phase 3 includes Wartezeit status check (3 months from start_date)
    - Phase 0 collects employment status, renter status, driver status to determine relevant modules
    - Phase 4 notes ongoing covered cases not voided by cancellation
    - Phase 5 spawns research agent with `insurance_type: rechtsschutz`
    - No YAML frontmatter, no execution_context, no file writes
    - File is 150-220 lines
  </acceptance_criteria>
  <done>rechtsschutz.md exists with module-based coverage comparison (not sum-based), Wartezeit check, and life-situation-aware module recommendations</done>
</task>

</tasks>

<verification>
All 3 sub-skill files exist and follow the sub-skill contract:
```bash
for f in haftpflicht hausrat rechtsschutz; do
  echo "=== $f ==="
  test -f "skills/insurance/sub-skills/$f.md" && echo "EXISTS" || echo "MISSING"
  head -1 "skills/insurance/sub-skills/$f.md"
  grep -c "Phase [0-6]" "skills/insurance/sub-skills/$f.md"
  grep -q "finyx-insurance-research-agent" "skills/insurance/sub-skills/$f.md" && echo "HAS AGENT SPAWN" || echo "MISSING AGENT SPAWN"
  grep -q "sonderkundigungsrecht" "skills/insurance/sub-skills/$f.md" && echo "HAS CANCELLATION" || echo "MISSING CANCELLATION"
done
```
</verification>

<success_criteria>
- 3 sub-skill files created at skills/insurance/sub-skills/{haftpflicht,hausrat,rechtsschutz}.md
- Each file follows the 7-phase structure (Phase 0-6)
- Each file spawns finyx-insurance-research-agent via Task
- Each file includes cancellation deadline tracking with alert banners
- No file has YAML frontmatter or execution_context
- Haftpflicht uses sum-based benchmark (≥€5M Deckungssumme)
- Hausrat computes benchmark from living area (€650/m²)
- Rechtsschutz uses module-based comparison (not monetary sum)
</success_criteria>

<output>
After completion, create `.planning/phases/21-per-type-sub-skills-tier-1-2/21-01-SUMMARY.md`
</output>
