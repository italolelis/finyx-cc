---
phase: 17-integration-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - package.json
  - README.md
autonomous: true
requirements: [CLEAN-03, INTG-03]

must_haves:
  truths:
    - "README.md documents plugin installation via claude plugin add"
    - "bin/install.js either removed or updated to install skills/ layout"
    - "package.json files field reflects current directory structure"
  artifacts:
    - path: "README.md"
      provides: "Plugin installation instructions"
      contains: "claude plugin add"
    - path: "package.json"
      provides: "Updated package manifest"
  key_links:
    - from: "README.md"
      to: ".claude-plugin/plugin.json"
      via: "Installation instructions reference plugin system"
      pattern: "claude plugin add|claude plugin install"
---

<objective>
Update distribution artifacts for the plugin model: rewrite README with plugin install instructions, update package.json to reflect skills/ layout, and handle bin/install.js (npm fallback or removal).

Purpose: Users need clear instructions to install Finyx as a Claude Code plugin. The npm installer either adapts to the new layout or is removed in favor of plugin-only distribution.
Output: Updated README.md, package.json, and bin/install.js reflecting the v2.0 plugin architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-integration-distribution/17-CONTEXT.md

<interfaces>
From .claude-plugin/plugin.json:
```json
{
  "name": "finyx",
  "version": "2.0.0",
  "description": "AI-powered personal finance advisor for Germany and Brazil",
  "author": { "name": "Italo Vietro" },
  "homepage": "https://github.com/italolelis/finyx",
  "repository": "https://github.com/italolelis/finyx",
  "license": "MIT"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bin/install.js and package.json for plugin distribution</name>
  <files>bin/install.js, package.json</files>
  <read_first>
    - bin/install.js (full file — 256 lines)
    - package.json
    - .claude-plugin/plugin.json
  </read_first>
  <action>
**Decision: Remove bin/install.js.** The REQUIREMENTS.md "Out of Scope" section explicitly states: "npm-only fallback distribution — Plugin system replaces npm as primary — clean break." The plugin system (`claude plugin add`) replaces `npx finyx-cc`. Keeping a broken installer creates confusion.

1. Delete `bin/install.js`:
   ```bash
   rm bin/install.js
   rmdir bin/ 2>/dev/null
   ```

2. Update `package.json`:
   - Remove `"bin"` field (no more CLI entry point)
   - Update `"files"` field to: `["skills", ".claude-plugin"]`
   - Update `"version"` to `"2.0.0"` to match plugin.json
   - Remove `"name": "finyx-cc"` — update to `"name": "finyx"` (or keep finyx-cc for npm registry reservation, but add a note)
   - Keep the package.json for repository metadata but it's no longer the primary distribution mechanism

3. Verify no CI workflows reference `bin/install.js`:
   ```bash
   grep -r "install.js\|finyx-cc" .github/ 2>/dev/null
   ```
   If the publish workflow references the old binary, update or remove it.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/italovietro/projects/immo && test ! -f bin/install.js && node -e "const p=require(\"./package.json\"); if(!p.files.includes(\"skills\")) process.exit(1); if(!p.files.includes(\".claude-plugin\")) process.exit(1); console.log(\"package.json OK\")"'</automated>
  </verify>
  <acceptance_criteria>
    - `bin/install.js` does not exist
    - `package.json` `files` field includes `skills` and `.claude-plugin`
    - `package.json` `version` is `2.0.0`
    - No `"bin"` field in package.json
  </acceptance_criteria>
  <done>npm installer removed. package.json reflects plugin architecture. CLEAN-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for plugin installation</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file)
    - .claude-plugin/plugin.json
  </read_first>
  <action>
Rewrite README.md installation and quick-start sections. Keep the existing feature descriptions and command reference — only update:

1. **Installation section** — Replace npx instructions with:
   ```
   ## Installation

   Finyx is a Claude Code plugin. Install it with:

   ```bash
   claude plugin add github:italolelis/finyx
   ```

   Requires [Claude Code](https://docs.anthropic.com/en/docs/claude-code) installed on your machine.
   ```

2. **Remove uninstall section** referencing `npx finyx-cc --uninstall` — plugin removal is handled by Claude Code's plugin system.

3. **Update the "What it does" section** if it mentions npm distribution — replace with plugin terminology.

4. **Add a "Migrating from v1.x" section** at the bottom:
   ```
   ## Migrating from v1.x

   If you previously installed via `npx finyx-cc`, uninstall first:
   ```bash
   npx finyx-cc --uninstall
   ```
   Then install the plugin:
   ```bash
   claude plugin add github:italolelis/finyx
   ```
   ```

5. Keep all command documentation, feature lists, and the existing structure intact. Only change distribution-related content.

6. Update any references from `/fin:*` to `/finyx:*` if inconsistent. The current plugin uses `finyx` prefix.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/italovietro/projects/immo && grep -q "claude plugin add" README.md && ! grep -q "npx finyx-cc --global" README.md && echo "README OK"'</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `claude plugin add github:italolelis/finyx`
    - README.md does NOT contain `npx finyx-cc --global` as primary install
    - Migration section exists for v1.x users
    - All `/finyx:*` command documentation preserved
  </acceptance_criteria>
  <done>README reflects plugin installation. Users can follow instructions to install Finyx. INTG-03 documentation satisfied.</done>
</task>

</tasks>

<verification>
```bash
# bin/install.js gone
test ! -f bin/install.js && echo "PASS: installer removed"

# package.json updated
node -e "const p=require('./package.json'); console.log('version:', p.version); console.log('files:', p.files); console.log('bin:', p.bin || 'none')"

# README has plugin instructions
grep "claude plugin add" README.md && echo "PASS: plugin install documented"
```
</verification>

<success_criteria>
- bin/install.js removed, package.json updated with skills/ layout
- README.md documents `claude plugin add github:italolelis/finyx` as primary installation
- Migration path documented for v1.x users
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-distribution/17-02-SUMMARY.md`
</output>
