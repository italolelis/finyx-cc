---
phase: 17-integration-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/realestate/references/briefing.md
  - skills/realestate/references/location-research.md
  - commands/finyx/ (deleted)
  - agents/ (deleted)
  - finyx/ (deleted)
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "commands/finyx/ directory does not exist in the repository"
    - "agents/ root directory does not exist in the repository"
    - "finyx/references/ and finyx/templates/ directories do not exist"
    - "All reference/template content from finyx/ exists in its target skill"
  artifacts:
    - path: "skills/realestate/references/briefing.md"
      provides: "Briefing template migrated from finyx/templates/"
      min_lines: 10
  key_links:
    - from: "skills/realestate/SKILL.md"
      to: "skills/realestate/references/briefing.md"
      via: "${CLAUDE_SKILL_DIR}/references/briefing.md reference"
      pattern: "CLAUDE_SKILL_DIR.*references/briefing"
---

<objective>
Verify all legacy content has been migrated to skills, then remove the three legacy directories: `commands/finyx/`, `agents/`, and `finyx/`.

Purpose: Clean break from the old command-based architecture. These directories are now fully superseded by `skills/`.
Output: Legacy directories deleted from repository, missing template migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-integration-distribution/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate missing templates and verify migration completeness</name>
  <files>skills/realestate/references/briefing.md</files>
  <read_first>
    - finyx/templates/briefing.md
    - finyx/templates/location-research.md
    - finyx/templates/config.json
    - finyx/templates/state.md
    - finyx/templates/profile.json
    - skills/realestate/SKILL.md (line 1649 — references briefing.md)
    - skills/profile/references/state.md
    - skills/profile/references/profile.json
  </read_first>
  <action>
1. Copy `finyx/templates/briefing.md` to `skills/realestate/references/briefing.md` — the realestate SKILL.md at line 1649 references `${CLAUDE_SKILL_DIR}/references/briefing.md` but the file is missing.

2. Verify every `finyx/references/` file already exists in its target skill by running diff checks:
   - `finyx/references/disclaimer.md` → already in skills/insights/, skills/insurance/, skills/invest/, skills/pension/, skills/profile/, skills/realestate/, skills/tax/ references
   - `finyx/references/methodology.md` → skills/realestate/references/
   - `finyx/references/erbpacht-detection.md` → skills/realestate/references/
   - `finyx/references/transport-assessment.md` → skills/realestate/references/
   - `finyx/references/germany/tax-rules.md` → skills/tax/references/, skills/realestate/references/, skills/profile/references/
   - `finyx/references/germany/tax-investment.md` → skills/tax/references/
   - `finyx/references/germany/brokers.md` → skills/invest/references/
   - `finyx/references/germany/pension.md` → skills/pension/references/
   - `finyx/references/germany/health-insurance.md` → skills/insurance/references/
   - `finyx/references/brazil/tax-investment.md` → skills/tax/references/
   - `finyx/references/brazil/brokers.md` → skills/invest/references/
   - `finyx/references/brazil/pension.md` → skills/pension/references/
   - `finyx/references/insights/benchmarks.md` → skills/insights/references/
   - `finyx/references/insights/scoring-rules.md` → skills/insights/references/

3. Verify templates migration:
   - `finyx/templates/profile.json` → skills/profile/references/profile.json
   - `finyx/templates/state.md` → skills/profile/references/state.md
   - `finyx/templates/briefing.md` → skills/realestate/references/briefing.md (just copied)
   - `finyx/templates/config.json` — legacy immo config, NOT needed by any skill (realestate uses .immo/config.json at runtime, profile uses .finyx/profile.json)
   - `finyx/templates/location-research.md` — check if any SKILL.md references it. If not referenced, it is dead content and safe to drop.

4. For each file, run: `diff finyx/references/X skills/Y/references/X` to confirm content match. Log any mismatches.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/italovietro/projects/immo && test -f skills/realestate/references/briefing.md && echo "briefing.md migrated OK" && diff -q finyx/references/disclaimer.md skills/insights/references/disclaimer.md && echo "disclaimer OK" && diff -q finyx/references/methodology.md skills/realestate/references/methodology.md && echo "methodology OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `skills/realestate/references/briefing.md` exists and matches `finyx/templates/briefing.md`
    - All 14 reference files verified present in their target skills
    - Any content mismatches documented
  </acceptance_criteria>
  <done>Every legacy reference/template has a verified copy in its target skill. briefing.md gap is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy directories</name>
  <files>commands/finyx/ (deleted), agents/ (deleted), finyx/ (deleted)</files>
  <read_first>
    - Task 1 verification output (migration confirmed)
  </read_first>
  <action>
IMPORTANT: This task is DESTRUCTIVE. Only proceed after Task 1 confirms all content is migrated.

1. Remove `commands/finyx/` directory:
   ```bash
   rm -rf commands/finyx/
   ```
   If `commands/` directory is now empty, remove it too: `rmdir commands/ 2>/dev/null`

2. Remove root `agents/` directory:
   ```bash
   rm -rf agents/
   ```

3. Remove `finyx/` directory (references + templates):
   ```bash
   rm -rf finyx/
   ```

4. Verify no remaining references to deleted paths in the codebase:
   ```bash
   grep -r "commands/finyx" skills/ .claude-plugin/ 2>/dev/null
   grep -r "agents/finyx" skills/ .claude-plugin/ 2>/dev/null
   grep -r "finyx/references" skills/ .claude-plugin/ 2>/dev/null
   grep -r "finyx/templates" skills/ .claude-plugin/ 2>/dev/null
   ```
   All four greps must return empty.

5. Run `git add -A commands/ agents/ finyx/` to stage deletions.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/italovietro/projects/immo && test ! -d commands/finyx && test ! -d agents && test ! -d finyx && echo "ALL LEGACY DIRS REMOVED"'</automated>
  </verify>
  <acceptance_criteria>
    - `ls commands/finyx/` returns "No such file or directory"
    - `ls agents/` returns "No such file or directory"
    - `ls finyx/` returns "No such file or directory"
    - No dangling references to deleted paths in skills/ or .claude-plugin/
  </acceptance_criteria>
  <done>All three legacy directories are deleted. No code references them. CLEAN-01 and CLEAN-02 satisfied.</done>
</task>

</tasks>

<verification>
```bash
# All legacy dirs gone
test ! -d commands/finyx && test ! -d agents && test ! -d finyx && echo "PASS: legacy removed"

# Migrated template exists
test -f skills/realestate/references/briefing.md && echo "PASS: briefing migrated"

# No dangling path references
grep -rn "commands/finyx\|agents/finyx\|finyx/references\|finyx/templates" skills/ .claude-plugin/ 2>/dev/null | grep -v ".planning" && echo "FAIL: dangling refs" || echo "PASS: no dangling refs"
```
</verification>

<success_criteria>
- commands/finyx/, agents/, and finyx/ directories no longer exist
- skills/realestate/references/briefing.md exists with full template content
- Zero dangling references to legacy paths in skills/ or .claude-plugin/
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-distribution/17-01-SUMMARY.md`
</output>
