---
phase: 01-foundation-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/install.js
  - commands/finyx/scout.md
  - commands/finyx/analyze.md
  - commands/finyx/compare.md
  - commands/finyx/filter.md
  - commands/finyx/rates.md
  - commands/finyx/stress-test.md
  - commands/finyx/status.md
  - commands/finyx/update.md
  - commands/finyx/report.md
  - commands/finyx/help.md
  - agents/finyx-analyzer-agent.md
  - agents/finyx-location-scout.md
  - agents/finyx-reporter-agent.md
  - finyx/references/methodology.md
  - finyx/references/erbpacht-detection.md
  - finyx/references/transport-assessment.md
  - finyx/references/germany/tax-rules.md
  - finyx/templates/config.json
  - finyx/templates/state.md
  - finyx/templates/briefing.md
  - finyx/templates/location-research.md
  - .github/workflows/publish.yml
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-04]

must_haves:
  truths:
    - "Package installs as finyx-cc via npx"
    - "All 10 existing RE commands register under /finyx:* namespace"
    - "All 3 agents are renamed to finyx-* prefix"
    - "Reference docs live under finyx/references/ with germany/ and brazil/ subdirs"
    - "No string 'immo' remains in any source file except CHANGELOG/history references"
    - "Old immo-cc npm package is deprecated with rename notice"
  artifacts:
    - path: "package.json"
      provides: "Package identity as finyx-cc"
      contains: "\"name\": \"finyx-cc\""
    - path: "bin/install.js"
      provides: "Installer with finyx paths and branding"
      contains: "FINYX"
    - path: "commands/finyx/analyze.md"
      provides: "Renamed analyze command"
      contains: "name: finyx:analyze"
    - path: "agents/finyx-analyzer-agent.md"
      provides: "Renamed analyzer agent"
      contains: "name: finyx-analyzer-agent"
    - path: "finyx/references/germany/tax-rules.md"
      provides: "German tax rules under new path"
    - path: "finyx/references/brazil/.gitkeep"
      provides: "Brazil reference doc placeholder"
  key_links:
    - from: "commands/finyx/*.md"
      to: "finyx/references/"
      via: "@path directives in execution_context"
      pattern: "@~/.claude/finyx/"
    - from: "bin/install.js"
      to: "commands/finyx/"
      via: "source directory mapping"
      pattern: "commands.*finyx"
    - from: "bin/install.js"
      to: "agents/finyx-*.md"
      via: "agent prefix filter in uninstall"
      pattern: "startsWith.*finyx-"
---

<objective>
Rename the entire project from immo-cc to finyx-cc. Move all directories, update all internal references, rebrand the installer, create the brazil/ reference doc placeholder, and deprecate the old npm package.

Purpose: Establishes the Finyx brand identity and directory structure that all subsequent plans build on. Per D-01 through D-05, this is a hard cut with no coexistence period.
Output: All source files under new names/paths, zero references to old immo namespace in code, old package deprecated on npm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-profile/01-CONTEXT.md
@.planning/phases/01-foundation-profile/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories, files, and package identity</name>
  <files>
    package.json, bin/install.js, .github/workflows/publish.yml,
    commands/finyx/ (all 10 .md files, moved from commands/immo/),
    agents/finyx-analyzer-agent.md, agents/finyx-location-scout.md, agents/finyx-reporter-agent.md (renamed from immo-*),
    finyx/ (entire directory tree, moved from immo/),
    finyx/references/brazil/.gitkeep (new)
  </files>
  <read_first>
    package.json
    bin/install.js
    .github/workflows/publish.yml
    commands/immo/analyze.md
    commands/immo/scout.md
    commands/immo/compare.md
    commands/immo/filter.md
    commands/immo/rates.md
    commands/immo/stress-test.md
    commands/immo/status.md
    commands/immo/update.md
    commands/immo/report.md
    commands/immo/help.md
    agents/immo-analyzer-agent.md
    agents/immo-location-scout.md
    agents/immo-reporter-agent.md
    immo/references/methodology.md
    immo/references/germany/tax-rules.md
  </read_first>
  <action>
    **Per D-01, D-03, D-04, D-05: Hard cut rename, no coexistence.**

    NOTE: Do NOT rename/move `commands/immo/init.md` — it is replaced by `finyx:profile` in Plan 03. Delete it after moving other commands.

    1. **Move directory trees:**
       - `commands/immo/` -> `commands/finyx/` (copy all .md EXCEPT init.md, then delete commands/immo/)
       - `immo/` -> `finyx/` (full recursive move)
       - Rename agent files: `agents/immo-analyzer-agent.md` -> `agents/finyx-analyzer-agent.md`, same for location-scout and reporter

    2. **Create brazil placeholder (per FOUND-04):**
       - Create `finyx/references/brazil/.gitkeep` (empty file)

    3. **Update package.json:**
       ```json
       {
         "name": "finyx-cc",
         "description": "A comprehensive AI-powered personal finance advisor for Claude Code — covering tax, investments, insurance, pensions, and real estate.",
         "bin": {
           "finyx-cc": "bin/install.js"
         },
         "files": ["bin", "commands", "finyx", "agents", "scripts"],
         "keywords": ["claude", "claude-code", "ai", "finance", "personal-finance", "tax", "investment", "pension", "real-estate", "germany", "brazil"],
         "homepage": "https://github.com/italolelis/immo",
         "repository": { "type": "git", "url": "git+https://github.com/italolelis/immo.git" }
       }
       ```
       Keep version, author, license, engines, bugs unchanged.

    4. **Update bin/install.js:**
       - Replace ASCII banner with FINYX branding (new ASCII art or simple text banner with "FINYX")
       - Change all display strings: "IMMO" -> "FINYX", "immo-cc" -> "finyx-cc", "/immo:" -> "/finyx:"
       - Update source directory mappings in `install()`:
         - `path.join(src, 'commands', 'immo')` -> `path.join(src, 'commands', 'finyx')`
         - `path.join(targetDir, 'commands', 'immo')` -> `path.join(targetDir, 'commands', 'finyx')`
         - `path.join(src, 'immo')` -> `path.join(src, 'finyx')`
         - `path.join(targetDir, 'immo')` -> `path.join(targetDir, 'finyx')`
       - Update VERSION file path: `path.join(targetDir, 'immo', 'VERSION')` -> `path.join(targetDir, 'finyx', 'VERSION')`
       - Update `uninstall()`:
         - `path.join(targetDir, 'commands', 'immo')` -> `path.join(targetDir, 'commands', 'finyx')`
         - `path.join(targetDir, 'immo')` -> `path.join(targetDir, 'finyx')`
         - Agent prefix filter: `file.startsWith('immo-')` -> `file.startsWith('finyx-')`
       - Update help text: `npx immo-cc` -> `npx finyx-cc`, `/immo:help` -> `/finyx:help`, `/immo:init` -> `/finyx:profile`, `/immo:scout` -> `/finyx:scout`
       - Quick Start steps update: reference `/finyx:profile` instead of `/immo:init`, `.finyx/` instead of `.immo/`

    5. **Update YAML frontmatter `name:` field in all 10 command files:**
       - `commands/finyx/scout.md`: `name: finyx:scout`
       - `commands/finyx/analyze.md`: `name: finyx:analyze`
       - `commands/finyx/compare.md`: `name: finyx:compare`
       - `commands/finyx/filter.md`: `name: finyx:filter`
       - `commands/finyx/rates.md`: `name: finyx:rates`
       - `commands/finyx/stress-test.md`: `name: finyx:stress-test`
       - `commands/finyx/status.md`: `name: finyx:status`
       - `commands/finyx/update.md`: `name: finyx:update`
       - `commands/finyx/report.md`: `name: finyx:report`
       - `commands/finyx/help.md`: `name: finyx:help`

    6. **Update all @path references inside command and agent files:**
       - Replace every `@~/.claude/immo/` with `@~/.claude/finyx/`
       - Replace every `/immo:` with `/finyx:` in user-facing text, instructions, and cross-references
       - Replace every `.immo/` with `.finyx/` in bash code blocks and path references
       - Replace every `IMMO` with `FINYX` in banner text (the `━━━` separator banners)

    7. **Update agent files:**
       - `agents/finyx-analyzer-agent.md`: Update `name:` frontmatter to `finyx-analyzer-agent`, all @path references from `immo/` to `finyx/`, all `.immo/` to `.finyx/`
       - Same for `finyx-location-scout.md` and `finyx-reporter-agent.md`

    8. **Update .github/workflows/publish.yml:**
       - If it references `immo-cc` package name, update to `finyx-cc`

    9. **Final sweep:** Run `grep -r "immo" commands/ finyx/ agents/ bin/ package.json` and fix any remaining references. The ONLY acceptable remaining `immo` strings are in git history references or the word "Immobilien" if used in German context within reference docs.
  </action>
  <verify>
    <automated>grep -rn "immo" commands/finyx/ agents/ bin/install.js package.json --include="*.md" --include="*.js" --include="*.json" | grep -v "Immobilien" | grep -v ".planning/" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "finyx-cc"` and `"finyx-cc": "bin/install.js"` in bin field
    - `commands/immo/` directory does not exist
    - `immo/` directory does not exist (at project root)
    - `commands/finyx/` contains exactly 10 .md files (scout, analyze, compare, filter, rates, stress-test, status, update, report, help)
    - Every .md file in `commands/finyx/` has `name: finyx:` prefix in YAML frontmatter
    - `agents/` contains `finyx-analyzer-agent.md`, `finyx-location-scout.md`, `finyx-reporter-agent.md`
    - No files named `immo-*` exist in `agents/`
    - `finyx/references/germany/tax-rules.md` exists
    - `finyx/references/brazil/.gitkeep` exists
    - `finyx/references/methodology.md` exists
    - `bin/install.js` contains `FINYX` (banner) and `commands', 'finyx'` (path mapping)
    - `bin/install.js` contains `startsWith('finyx-')` (agent prefix filter)
    - `grep -rn "immo" commands/ agents/ bin/ package.json | grep -v Immobilien` returns 0 results
  </acceptance_criteria>
  <done>All project files renamed from immo to finyx. Package publishes as finyx-cc. All 10 RE commands register under /finyx:* namespace. Brazil reference dir placeholder exists. Zero remaining immo references in code.</done>
</task>

<task type="checkpoint:human-action" gate="non-blocking">
  <name>Task 2: Deprecate immo-cc on npm (per D-02)</name>
  <action>
    **Per D-02: Run `npm deprecate immo-cc "Renamed to finyx-cc"` to signal the old package is dead.**

    This requires npm publish credentials for the immo-cc package. Claude cannot authenticate to npm on the user's behalf.
  </action>
  <instructions>
    Run this command in your terminal (you must be logged into npm with publish access to immo-cc):

    ```bash
    npm deprecate immo-cc "Renamed to finyx-cc. Install finyx-cc instead: npx finyx-cc"
    ```

    Verify it worked:
    ```bash
    npm view immo-cc deprecated
    ```

    Expected output should show the deprecation message.
  </instructions>
  <verify>
    <automated>npm view immo-cc deprecated 2>/dev/null | grep -q "finyx-cc" && echo "PASS" || echo "SKIP — run npm deprecate manually when ready"</automated>
  </verify>
  <done>immo-cc npm package shows deprecation notice pointing users to finyx-cc.</done>
  <resume-signal>Type "done" after running npm deprecate, or "skip" to defer until after first finyx-cc publish</resume-signal>
</task>

</tasks>

<verification>
- `node bin/install.js --help` runs without error and shows FINYX branding
- `grep -rn "immo" commands/ finyx/ agents/ bin/ package.json | grep -v Immobilien` returns empty
- `ls commands/finyx/*.md | wc -l` equals 10
- `ls agents/finyx-*.md | wc -l` equals 3
- `ls finyx/references/brazil/.gitkeep` exists
</verification>

<success_criteria>
All source files renamed from immo to finyx namespace. Package identity is finyx-cc. All 10 RE commands are under /finyx:* with identical behavior (per D-05). Brazil reference doc directory created (per FOUND-04). Installer shows FINYX branding and handles finyx paths. Old immo-cc package deprecated on npm (per D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-profile/01-01-SUMMARY.md`
</output>
