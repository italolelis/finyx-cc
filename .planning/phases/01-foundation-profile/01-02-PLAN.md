---
phase: 01-foundation-profile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - finyx/templates/profile.json
  - finyx/references/disclaimer.md
autonomous: true
requirements: [FOUND-03, FOUND-05, PROF-04]

must_haves:
  truths:
    - "Profile schema template exists with all IMMO fields preserved at original paths"
    - "Profile schema includes new financial fields (identity, countries, goals)"
    - "Disclaimer template exists as standalone Markdown file"
  artifacts:
    - path: "finyx/templates/profile.json"
      provides: "Profile schema template for /finyx:profile command"
      contains: "\"investor\""
    - path: "finyx/references/disclaimer.md"
      provides: "Legal disclaimer template for all advisory outputs"
      contains: "does not constitute"
  key_links:
    - from: "finyx/templates/profile.json"
      to: "commands/finyx/profile.md"
      via: "@path include in execution_context"
      pattern: "@~/.claude/finyx/templates/profile.json"
    - from: "finyx/references/disclaimer.md"
      to: "commands/finyx/*.md"
      via: "@path include in execution_context"
      pattern: "@~/.claude/finyx/references/disclaimer.md"
---

<objective>
Create the profile.json schema template and legal disclaimer template. These are foundational artifacts consumed by Plan 03 (profile command) and wired into all commands.

Purpose: Defines the shared data contract (profile.json) that all specialist agents read, and the legal disclaimer (PROF-05) that must appear on all advisory outputs.
Output: Two new template files under finyx/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-profile/01-CONTEXT.md
@.planning/phases/01-foundation-profile/01-RESEARCH.md

<interfaces>
<!-- Existing config.json schema that must be preserved in profile.json -->
From immo/templates/config.json:
```json
{
  "investor": {
    "country": "Germany",
    "taxClass": "I",
    "churchTax": false,
    "children": 0,
    "income": { "primary": 150000, "spouse": 0, "total": 150000 },
    "marginalRate": 44.31,
    "liquidAssets": 100000,
    "monthlyCommitments": 500
  },
  "strategy": { "type": "neubau-rent-sell", "horizon": 10, "financing": { "ltv": 100, "fixedPeriod": 10 }, "management": "professional", "exitPlan": "sell-tax-free" },
  "criteria": { "propertyType": "...", "bedrooms": [...], "minYield": 2.8, ... },
  "assumptions": { "appreciation": 2.0, "rentIncrease": 0, ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile.json schema template</name>
  <files>finyx/templates/profile.json</files>
  <read_first>
    immo/templates/config.json
  </read_first>
  <action>
    **Per D-09: Full merge. Per D-11: Preserve all IMMO fields at original paths.**

    Create `finyx/templates/profile.json` with the following exact structure:

    ```json
    {
      "$schema": "https://finyx.dev/schemas/profile.schema.json",
      "version": "1.0.0",
      "created": "ISO_DATE",
      "updated": "ISO_DATE",

      "identity": {
        "residence_country": null,
        "nationality": null,
        "income_countries": [],
        "has_income_in_multiple_countries": false,
        "cross_border": false,
        "family_status": null,
        "children": 0
      },

      "countries": {
        "germany": {
          "tax_class": null,
          "church_tax": false,
          "gross_income": 0,
          "marginal_rate": 0
        },
        "brazil": {
          "ir_regime": null,
          "gross_income": 0,
          "cpf": null
        }
      },

      "goals": {
        "risk_tolerance": null,
        "investment_horizon": 0,
        "primary_goals": []
      },

      "project": {
        "name": "",
        "propertiesFolder": "properties",
        "outputFolder": ".finyx/output",
        "language": "en"
      },

      "investor": {
        "country": "",
        "taxClass": "",
        "churchTax": false,
        "children": 0,
        "income": {
          "primary": 0,
          "spouse": 0,
          "total": 0
        },
        "marginalRate": 0,
        "liquidAssets": 0,
        "monthlyCommitments": 0
      },

      "strategy": {
        "type": "neubau-rent-sell",
        "horizon": 10,
        "financing": {
          "ltv": 100,
          "fixedPeriod": 10
        },
        "management": "professional",
        "exitPlan": "sell-tax-free"
      },

      "criteria": {
        "propertyType": "2-bedroom apartment",
        "bedrooms": [2],
        "minYield": 2.8,
        "maxPrice": 450000,
        "minSize": 45,
        "maxSize": 80,
        "parkingRequired": false,
        "parkingPreferred": true,
        "excludeErbpacht": true,
        "excludeGroundFloor": false,
        "topFloorPreferred": false,
        "newConstructionOnly": true
      },

      "assumptions": {
        "appreciation": 2.0,
        "rentIncrease": 0,
        "vacancy": 0,
        "verwaltungPerSqm": 1.0,
        "rucklagePerSqm": 0.6,
        "saleCosts": 7.0,
        "constructionPeriod": 18,
        "constructionDraw": 50
      }
    }
    ```

    Key design decisions:
    - `identity`, `countries`, `goals` are NEW top-level keys for financial profile data
    - `project`, `investor`, `strategy`, `criteria`, `assumptions` are PRESERVED exactly as in config.json — same field names, same nesting, same camelCase convention (per D-11)
    - `identity.cross_border` is derived (per D-08), stored as boolean, computed by the profile command
    - `countries.germany` and `countries.brazil` hold country-specific tax fields
    - The `investor` section duplicates some data from `countries` (e.g., marginalRate mirrors countries.germany.marginal_rate) — this is intentional for backward compatibility with RE commands that read `investor.marginalRate`
    - `project.outputFolder` updated from `.immo/output` to `.finyx/output` (per D-12)

    NOTE: If Plan 01 has not yet executed, this file will be created directly at `finyx/templates/profile.json`. If the directory doesn't exist yet, create it. Both plans are Wave 1 and touch different files, so no conflict.
  </action>
  <verify>
    <automated>node -e "const p = require('./finyx/templates/profile.json'); const checks = [p.investor && p.investor.marginalRate !== undefined, p.strategy && p.strategy.horizon !== undefined, p.criteria && p.criteria.minYield !== undefined, p.assumptions && p.assumptions.appreciation !== undefined, p.identity && p.identity.cross_border !== undefined, p.countries && p.countries.germany && p.countries.brazil, p.goals && p.goals.risk_tolerance !== undefined]; if (checks.every(Boolean)) { console.log('PASS'); process.exit(0); } else { console.log('FAIL', checks); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `finyx/templates/profile.json` exists and is valid JSON
    - Contains top-level keys: `$schema`, `version`, `created`, `updated`, `identity`, `countries`, `goals`, `project`, `investor`, `strategy`, `criteria`, `assumptions`
    - `investor.marginalRate` field exists (backward compat)
    - `investor.liquidAssets` field exists (backward compat)
    - `strategy.horizon` field exists (backward compat)
    - `criteria.minYield` field exists (backward compat)
    - `assumptions.appreciation` field exists (backward compat)
    - `identity.cross_border` field exists
    - `identity.residence_country` field exists
    - `countries.germany.tax_class` field exists
    - `countries.brazil.ir_regime` field exists
    - `goals.risk_tolerance` field exists
    - `project.outputFolder` equals `.finyx/output` (not `.immo/output`)
  </acceptance_criteria>
  <done>Profile schema template exists with all existing IMMO fields preserved at original paths AND new financial profile fields added. RE commands can read investor/strategy/criteria/assumptions without changes.</done>
</task>

<task type="auto">
  <name>Task 2: Create legal disclaimer template</name>
  <files>finyx/references/disclaimer.md</files>
  <read_first>
    immo/references/methodology.md
  </read_first>
  <action>
    **Per FOUND-05 and PROF-05: Disclaimer template as cross-cutting concern.**

    Create `finyx/references/disclaimer.md` with the following exact content:

    ```markdown
    ## Legal Disclaimer

    Finyx provides financial information for educational and planning purposes only. Nothing in this output constitutes financial, tax, legal, or investment advice. All calculations, projections, and recommendations are estimates based on the information you provided and publicly available data.

    **You should:**
    - Consult a qualified tax advisor (Steuerberater / Contador) before making tax-related decisions
    - Consult a licensed financial advisor before making investment decisions
    - Verify all tax rules and rates with official sources (BMF, Receita Federal) as regulations change frequently
    - Not rely solely on this tool for financial planning

    Tax rules, market conditions, and regulations change frequently. Finyx reference documents are stamped with their applicable tax year — verify they match the current year before acting on any guidance.

    ---
    *Finyx is an open-source educational tool, not a licensed financial advisory service.*
    ```

    This file will be included in every command's `<execution_context>` via:
    ```
    @~/.claude/finyx/references/disclaimer.md
    ```

    And every command that produces advisory output must instruct the agent to append this disclaimer at the end of its output.

    NOTE: The actual wiring of this disclaimer into command files happens in Plan 03. This task only creates the template file.
  </action>
  <verify>
    <automated>test -f finyx/references/disclaimer.md && grep -q "does not constitute" finyx/references/disclaimer.md && grep -q "Steuerberater" finyx/references/disclaimer.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `finyx/references/disclaimer.md` exists
    - Contains "does not constitute" (core legal language)
    - Contains "Steuerberater" (German tax advisor reference)
    - Contains "Receita Federal" (Brazilian tax authority reference)
    - Contains "Finyx" branding (not "IMMO")
    - Contains "educational and planning purposes only"
    - File is pure Markdown with no frontmatter (it's a reference doc, not a command)
  </acceptance_criteria>
  <done>Legal disclaimer template exists at finyx/references/disclaimer.md, ready for @path inclusion in all advisory commands.</done>
</task>

</tasks>

<verification>
- `finyx/templates/profile.json` is valid JSON with all required fields
- `finyx/references/disclaimer.md` contains legal language for both DE and BR jurisdictions
- Both files are pure content (no command frontmatter)
</verification>

<success_criteria>
Profile schema template preserves all IMMO backward-compatible fields while adding new identity/countries/goals sections. Disclaimer template covers both German and Brazilian advisory contexts. Both artifacts are ready for consumption by Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-profile/01-02-SUMMARY.md`
</output>
