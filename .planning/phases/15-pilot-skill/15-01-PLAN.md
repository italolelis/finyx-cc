---
phase: 15-pilot-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tax/SKILL.md
  - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "skills/tax/SKILL.md has correct frontmatter with disable-model-invocation: true"
    - "Tax skill trigger description is under 250 chars and front-loaded"
    - "Tax-scoring agent lives under skills/tax/agents/ (already done in Phase 13)"
    - "Tax reference docs bundled under skills/tax/references/ with portable paths"
    - "No @~/.claude/ path strings in the tax skill"
    - "Conversion checklist exists for Phase 16 bulk migration"
  artifacts:
    - path: "skills/tax/SKILL.md"
      provides: "Full tax advisory skill"
      contains: "disable-model-invocation: true"
    - path: ".planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md"
      provides: "Repeatable conversion pattern for Phase 16"
  key_links:
    - from: "skills/tax/SKILL.md"
      to: "skills/tax/references/germany/tax-investment.md"
      via: "${CLAUDE_SKILL_DIR}/references/ path"
      pattern: "CLAUDE_SKILL_DIR.*references.*germany/tax-investment"
    - from: "skills/tax/SKILL.md"
      to: "skills/tax/agents/finyx-tax-scoring-agent.md"
      via: "Task tool delegation"
      pattern: "finyx-tax-scoring-agent"
---

<objective>
Convert the tax command stub (`skills/tax/SKILL.md`) into a fully working skill, then document the conversion pattern as a checklist for Phase 16 bulk migration.

Purpose: Validate the full skill conversion pattern (frontmatter, scoped agents, bundled refs, portable paths) on a real advisory skill before applying it to 15 remaining commands.
Output: Working tax skill + conversion checklist template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-profile-skill/14-01-SUMMARY.md

<interfaces>
<!-- Source command to convert -->
@commands/finyx/tax.md

<!-- Established pattern to follow -->
@skills/profile/SKILL.md

<!-- Already in place from Phase 13 -->
<!-- skills/tax/agents/finyx-tax-scoring-agent.md — already scoped -->
<!-- skills/tax/references/disclaimer.md — already bundled -->
<!-- skills/tax/references/germany/tax-investment.md — already bundled -->
<!-- skills/tax/references/germany/tax-rules.md — already bundled -->
<!-- skills/tax/references/brazil/tax-investment.md — already bundled -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert tax command to full SKILL.md</name>
  <read_first>
    - commands/finyx/tax.md (source command — full content to convert)
    - skills/profile/SKILL.md (established pattern — frontmatter format, section structure)
    - skills/tax/SKILL.md (current stub to overwrite)
  </read_first>
  <files>skills/tax/SKILL.md</files>
  <action>
Overwrite `skills/tax/SKILL.md` with the full tax advisory skill content converted from `commands/finyx/tax.md`. Follow the exact pattern established by `skills/profile/SKILL.md`.

**Frontmatter changes (per SKILL-01, SKILL-02, SKILL-05):**
- `name: finyx-tax` (keep as-is from stub)
- `description:` — Write a trigger description under 250 chars, front-loaded for model detection. Use: "Investment tax advisor for German and Brazilian investors — Abgeltungssteuer, Sparerpauschbetrag, Vorabpauschale, DARF, come-cotas, cross-border DBA guidance"
- `allowed-tools: [Read, Write, Bash, AskUserQuestion]` — match source command (no Task tool needed since tax-scoring agent is not invoked by this command)
- `disable-model-invocation: true` — REQUIRED for advisory skills (per SKILL-02)

**execution_context path changes (per SKILL-04):**
- All `${CLAUDE_SKILL_DIR}/references/` paths kept as-is (already portable from Phase 13 migration)
- The source command references `${CLAUDE_SKILL_DIR}/references/germany/health-insurance.md` for PKV section (Phase 3.6). This file does NOT exist in `skills/tax/references/`. Two options:
  - Option A: Copy the health-insurance.md into skills/tax/references/germany/ (duplicates across skills)
  - Option B: Remove the health-insurance.md reference and add a note in Phase 3.6 that the PKV section requires the insurance skill to be co-installed
  - Choose Option B — the tax skill should not duplicate insurance reference docs. In the SKILL.md Phase 3.6 section, add a note: "This subsection references health-insurance.md from the insurance skill. If not available, skip PKV deduction calculation and direct user to /finyx:insurance."
- Keep `@.finyx/profile.json` reference as-is (runtime user file)

**Body content:**
- Copy ALL content from `commands/finyx/tax.md` sections: objective, execution_context, process (Phases 1-6), error_handling, notes
- The content is already correct — no business logic changes needed
- Verify no `@~/.claude/` path strings remain (should already be clean from Phase 13)

**Structural verification after writing:**
- File must have YAML frontmatter with `disable-model-invocation: true`
- All `${CLAUDE_SKILL_DIR}/references/` paths must resolve to files that exist in `skills/tax/references/`
- No `@~/.claude/finyx/` paths anywhere in the file
  </action>
  <acceptance_criteria>
    - grep -q "disable-model-invocation: true" skills/tax/SKILL.md
    - grep -q "name: finyx-tax" skills/tax/SKILL.md
    - grep -c "CLAUDE_SKILL_DIR" skills/tax/SKILL.md returns 3 or more (portable paths)
    - grep -c "@~/.claude/" skills/tax/SKILL.md returns 0 (no legacy paths)
    - wc -l skills/tax/SKILL.md shows 400+ lines (full content, not stub)
    - grep -q "Abgeltungssteuer" skills/tax/SKILL.md (German tax content present)
    - grep -q "DARF" skills/tax/SKILL.md (Brazilian tax content present)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/italovietro/projects/immo && grep -q "disable-model-invocation: true" skills/tax/SKILL.md && grep -q "name: finyx-tax" skills/tax/SKILL.md && test $(grep -c "@~/.claude/" skills/tax/SKILL.md) -eq 0 && test $(wc -l < skills/tax/SKILL.md) -gt 400 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skills/tax/SKILL.md contains the complete tax advisory skill with correct frontmatter, disable-model-invocation: true, portable paths, and all 6 process phases from the source command</done>
</task>

<task type="auto">
  <name>Task 2: Document conversion checklist for Phase 16</name>
  <read_first>
    - skills/tax/SKILL.md (just-converted skill — verify pattern)
    - skills/profile/SKILL.md (first converted skill — compare patterns)
    - commands/finyx/tax.md (source — understand what changed)
  </read_first>
  <files>.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md</files>
  <action>
Create `.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md` — a step-by-step checklist that Phase 16 executors can follow to convert any remaining command to a skill. Derive from the actual work done in Phase 14 (profile) and this phase (tax).

**Checklist structure:**

```markdown
# Skill Conversion Checklist

Template for converting `commands/finyx/{name}.md` to `skills/{name}/SKILL.md`.
Derived from Phase 14 (profile) and Phase 15 (tax) pilot conversions.

## Pre-flight

- [ ] Verify `skills/{name}/` directory exists (created in Phase 13)
- [ ] Verify `skills/{name}/SKILL.md` stub exists
- [ ] Verify agents already copied to `skills/{name}/agents/` (Phase 13)
- [ ] Verify reference docs already in `skills/{name}/references/` (Phase 13)
- [ ] Identify cross-skill reference dependencies (files referenced but owned by another skill)

## Frontmatter

- [ ] `name: finyx-{name}` — matches original command name
- [ ] `description:` — Under 250 chars, front-loaded for model detection. Start with the domain noun, include key terms users would say
- [ ] `allowed-tools:` — Copy from source command. Remove `Task` if no agent delegation needed
- [ ] `disable-model-invocation: true` — REQUIRED for all advisory skills (tax, invest, pension, insurance, insights). OMIT for interview-based skills (profile) and action-based skills (realestate per D-07)

## Content Migration

- [ ] Copy <objective> section verbatim
- [ ] Copy <execution_context> — verify all paths use `${CLAUDE_SKILL_DIR}/references/`
- [ ] Copy <process> section — all phases verbatim
- [ ] Copy <error_handling> section verbatim
- [ ] Copy <notes> section verbatim
- [ ] For cross-skill references: add fallback note ("if not available, skip and direct to /finyx:{other}")

## Path Verification

- [ ] Zero occurrences of `@~/.claude/` in the file
- [ ] All `${CLAUDE_SKILL_DIR}/references/` paths resolve to files in `skills/{name}/references/`
- [ ] `@.finyx/profile.json` kept as-is (runtime user file, not a skill reference)

## Agent Scoping

- [ ] All agents for this skill exist under `skills/{name}/agents/`
- [ ] No skill-specific agents remain at project root `agents/`
- [ ] Agent file references within SKILL.md use relative or skill-scoped paths

## Validation

- [ ] `grep -q "disable-model-invocation: true" skills/{name}/SKILL.md` (if advisory)
- [ ] `grep -c "@~/.claude/" skills/{name}/SKILL.md` returns 0
- [ ] `wc -l < skills/{name}/SKILL.md` shows substantial content (not a stub)
- [ ] Key domain terms present (grep for 2-3 domain-specific terms)

## Skills requiring disable-model-invocation: true

tax, invest, broker, pension, insurance, insights

## Skills NOT requiring disable-model-invocation

profile (interview-based), realestate commands (scout, analyze, filter, compare, stress-test, report, init — per D-07)
```

Per SKILL-05 — this checklist IS the documented conversion pattern enabling Phase 16 bulk migration.
  </action>
  <acceptance_criteria>
    - test -f .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - grep -q "disable-model-invocation: true" .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - grep -q "CLAUDE_SKILL_DIR" .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - grep -q "Phase 16" .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - wc -l .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md shows 40+ lines
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/italovietro/projects/immo && test -f .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md && grep -q "disable-model-invocation" .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md && grep -q "Phase 16" .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Conversion checklist exists with all steps derived from actual profile + tax conversions, ready for Phase 16 bulk migration</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -q "disable-model-invocation: true" skills/tax/SKILL.md` — SKILL-02 satisfied
2. `test $(grep -c "@~/.claude/" skills/tax/SKILL.md) -eq 0` — no legacy paths
3. `test $(wc -l < skills/tax/SKILL.md) -gt 400` — full content, not stub
4. `ls skills/tax/agents/finyx-tax-scoring-agent.md` — SKILL-03 (agent scoped)
5. `ls skills/tax/references/germany/tax-investment.md` — SKILL-04 (refs bundled)
6. `test -f .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md` — SKILL-05
</verification>

<success_criteria>
- Tax skill is a fully working SKILL.md with all advisory content from the source command
- Frontmatter has disable-model-invocation: true and a trigger description under 250 chars
- All paths are portable (${CLAUDE_SKILL_DIR}/references/), zero legacy @~/.claude/ paths
- Agent and references already scoped from Phase 13 — verified still in place
- Conversion checklist documents the repeatable pattern for Phase 16
</success_criteria>

<output>
After completion, create `.planning/phases/15-pilot-skill/15-01-SUMMARY.md`
</output>
